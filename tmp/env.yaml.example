# ============================================================================
#  env.yaml —— 部署新环境「需要改」的变量(其余默认见 defaults.yaml)
#  顺序: 连接 → 全局开关 → 基础设施(Harbor/路径/运行时/VIP) → 系统初始化调参
# ============================================================================

# ---- 连接身份(逻辑见 group_vars/all/connection.yaml) ----
connect_as: root                         # 默认 root 免密直连(本项目常态)。首次未免密: 改 sudo 填下面 bootstrap_*, 或临时 -e connect_as=auto 探测
# 下面 3 个仅 connect_as=sudo(或 auto 探测回退到 用户+sudo)时用到; root 全程免密可不填; 密码回退需控制机装 sshpass
#bootstrap_user: user
#bootstrap_ssh_pass: "改成该用户SSH密码"
#bootstrap_become_pass: "改成sudo密码(通常同上)"

# ---- 在线 / 离线(全局开关) ----
# false=在线(公网/镜像源); true=离线(init 改用本地 /opt/*-pkgs 源)。统一跑 init.yaml, 只改这里。
is_offline: false

# ---- 离线预取范围(学 kk: 声明一次架构+系统, prefetch-all.sh 自动读取, 不用每次手打 ARCH=/DISTRO=) ----
# 不填=all(双架构、全发行版, = 现状)。优先级: 环境变量 ARCH=/DISTRO= > 这里 > all。
#offline_arch: amd64                    # amd64 | arm64 | all —— 本套集群的架构
#offline_distros: [ubuntu, kylin]       # all 或列表, 只对 packages(系统依赖包)层生效; 用不到的发行版就不下

# ---- Harbor(域名/地址/账号) ----
harbor_instances:
  - { dns: "harbor.local.clusters", ip: "172.171.0.206" }
harbor_admin_username: admin
harbor_admin_password: Harbor12345      # 建议改
password: root123                        # harbor 数据库密码,建议改
# harbor 里存 k8s 镜像的项目名; 不填默认 kubernetes。镜像前缀 registry = <harbor_dns>/<harbor_project>
#harbor_project: kubernetes

# ---- 路径/目录(数据盘挂载点 / 安装位置) ----
# container_base 是 containerd/kubelet 数据盘主挂载点, 【必填, 别删】——
#   它是本变量的【唯一定义处】(defaults.yaml 里没有兜底), 三个挂盘 playbook
#   (mount-nvme / mount-nvme-lvm / mount-nvme-lvm-containerfs)与 containerd-setup 都读这里,
#   改路径改这一处即可, 全体跟着走; containerd_root 会自动派生为 <container_base>/containerd。
#   删掉或注释会让上述 playbook 直接报 container_base undefined。
#   ⚠ 别在 playbook 的 play vars: 里重复定义它(play vars 优先级高于 group_vars, 会让这里的
#     改动对那个 playbook 静默失效, 而 containerd-setup 仍按这里走 -> 盘挂在 A、containerd
#     往 B 写、数据落回系统盘且不报错)。
container_base: "/var/lib/container"     # containerd/kubelet 数据盘主挂载点
data_root: /var/lib/docker               # docker 数据目录
harbor_copy_path: /data/harbor-install   # harbor 安装目录
data_volume: /data/harbor-data           # harbor 数据目录

# ---- 容器运行时 + 镜像加速 ----
# containerd 大版本决定 config.toml 格式: >=2 走 v3(pause 的键名是 sandbox),
# 1.x 走 v2(键名 sandbox_image)。由 containerd_config_version 自动派生, 不用手写。
containerd_version: "2.3.5"              # 可选 "1.7.32"(config v2) / "2.x"(config v3)
runc_version: "1.5.1"
# docker 层(本项目 docker 仅给 Harbor 用; 版本供 docker_arch_map 拼包名 + 离线下载脚本)
docker_version: "29.4.2"                 # docker 静态二进制包版本
buildx_version: "0.32.1"                 # docker buildx 插件版本
compose_version: "2.32.4"                # docker compose 插件版本(仅离线下载脚本用; docker_arch_map 的 compose 文件名不带版本)
cri_dockerd_version: "0.3.16"            # 仅 [cri_docker_nodes] 非空时用到; 改版本需重下离线包(文件名带版本号)
# sandbox(pause)镜像 —— 【默认不写】, 由 defaults.yaml 自动派生:
#   离线 <registry>/pause:<pause_image_version>, 在线 <kube_image_mirror>/pause:<pause_image_version>
#   (registry = <harbor_dns>/<harbor_project>; pause_image_version 按 k8s_version 从 k8s_compat 派生)
# ⚠ 只有【接管已有集群】才需要写: 那种集群的 pause 早已推在固定路径(如 kk 装的是
#   registry.cn-beijing.aliyuncs.com/kubesphereio/pause:3.9), 派生值必然与之不一致, 而
#   kube-common 的预检拿派生值与节点 config.toml 逐字比对, 不一致就拦下全部节点。
#   自己从零装的集群不存在这个问题(containerd-setup 与预检同源, 必然一致)。
#sandbox_image: harbor.local.clusters/kubernetes/pause:3.10.1
docker_mirrors:                          # 容器镜像加速(可选)
  - "https://docker.hlmirror.com"
  - "https://docker.1ms.run"
  - "https://docker.m.daocloud.io"
  - "https://proxy.1panel.live"

# ---- apt 代理(playbook/apt-proxy.yaml 用; 机房有 squid 之类才需要) ----
# 站点专属值, 写在这里而不是改 playbook —— 本文件生成的 env.yaml 在 .gitignore 里,
# 不入库, 所以改它不会和上游冲突。以前地址硬编码在 apt-proxy.yaml 的 vars 里,
# 每站点都得改那一行, 于是每次 git pull 都撞 "local changes would be overwritten"。
#apt_proxy_url: "http://10.14.73.20:3128"

# ============================================================================
#  Kubernetes / CNI / 网络 / etcd / 下载源
# ============================================================================

# ---- K8s 版本 & CNI ----
# 切 k8s 版本【只改这一处】, 用真实 patch 号(先 curl https://dl.k8s.io/release/stable-1.36.txt 查,
#   否则 kubeadm 二进制 404)。改完自动跟上的: coredns / pause / crictl / etcd
#   (defaults.yaml 的 k8s_compat 按小版本派生; 现有行 1.28 / 1.34 / 1.35 / 1.36)。
#   ⚠ 上新小版本【必须先加一行】k8s_compat, 别指望兜底 —— 缺行时 crictl_version 兜底成
#     default(k8s_version) 即补丁版号(如 1.35.8), 而 cri-tools 只发 minor.0, 必然 404;
#     coredns / etcd 也会拿到别的小版本的值(现网踩过, 见 defaults.yaml 那段注释)。
#     权威值: kubeadm config images list --kubernetes-version v<版本>
#   ⚠ 不会自动跟上、需自己查兼容性的: containerd / runc / CNI(calico/cilium/flannel)版本。
#   换版本前先确认离线物料: ls offline/artifacts/kubernetes/
k8s_version: "1.35.8"
kube_network_plugin: calico             # calico(manifest) / flannel(manifest) / cilium(helm)
# calico 封装: ipip(默认,不要求二层互通) / crosssubnet(同子网走 BGP,跨子网才 IPIP) /
#              vxlan(IPIP 被中间设备挡掉时用) / none(纯 BGP,性能最好但【要求节点二层互通】)
# 默认已是 ipip(defaults.yaml), 不写这行也一样; 只在要偏离默认时才写。
# ⚠ 节点确实二层互通、且在意那 20 字节封装开销时才考虑 crosssubnet / none。
calico_encapsulation: ipip
# calico-node 探测本机 IP 的方式。默认 first-found(按网卡顺序取第一个非本地地址)。
# ⚠ 多网卡机器容易挑错, 症状像"CNI 没装好"。多网卡建议设 需要，如：interface=bond0
#   也支持 can-reach= / cidr= / kubernetes-internal-ip(跟随 Node InternalIP)。
#   ⚠ 改这个会让 calico-node 重启重新选址, 已跑起来且挑对了的别动。
#calico_ip_autodetection_method: interface=bond0
# Pod 带宽限速(kubernetes.io/{ingress,egress}-bandwidth 注解)。默认关。
# ⚠ 开之前必须确认各节点 /opt/cni/bin/bandwidth 存在 —— calico-cni 镜像【不含】该插件,
#   它属于上游 containernetworking/plugins。置 true 后 kube-common 会自动装该包;
#   若手工改了 conflist 却没装插件, 全节点 Pod 都建不了 sandbox:
#     plugin type="bandwidth" failed (add): failed to find plugin "bandwidth"
#calico_enable_bandwidth: false
#cni_plugins_version: "1.6.2"            # 上一项为 true 时才用到
# ---- Pod / Service 网段(⚠ 建完集群不可改, defaults 无兜底, 缺了 assert 会拦) ----
# 开双池时四项一体: pod_subnet 必须覆盖两个池段, 且三段两两不重叠。
# 别在文件别处再写一遍这两项 —— YAML 重复 key 静默取最后一个, 上面的会无声失效。
pod_subnet: 10.240.0.0/12                # ⚠ 开双池必须改! 见下面【第 0 步】
service_subnet: 10.96.0.0/12

# ---- GPU/CPU 分 Pod 网段(可选, 默认关) ----
# 按节点 hardware=gpu/cpu 标签分池, 标签依 inventory 的 accelerator_type 自动打。
# 超网算法与验算脚本见 docs/部署说明.md 十三.1。
# ⚠ 取消注释【只删行首那一个 #】, 别按块整段取消(说明行会变裸标量, YAML 直接报错)。
#
# ⚠⚠ 【第 0 步】开双池前先回上面把 pod_subnet 改成覆盖两池的超网(本例 10.14.0.0/17)。
#   只取消下面几行的注释、pod_subnet 留着默认的 10.240.0.0/12, 装出来的集群【能跑但是错的】:
#   Pod 从 10.14.x 取地址, 而 kube-proxy 的 clusterCIDR 是 10.240.0.0/12(来自 podSubnet),
#   于是 nft 规则里那句 `ip saddr != 10.240.0.0/12 jump mark-for-masquerade` 对所有 Pod 成立
#   —— Pod 访问任何 ClusterIP 都被 SNAT 成节点 IP。后果:
#     · 后端拿不到真实源 Pod IP;
#     · 按源 Pod/命名空间选择器写的 NetworkPolicy 对经 Service 的流量失效;
#     · 多一层 SNAT + conntrack, 高并发下有端口耗尽风险。
#   连通性完全正常, 所以功能测试全过也发现不了。2026-09-17 现网踩过(cpu200~203 那套)。
#   自洽的一组: pod_subnet 10.14.0.0/17 + 下面两池 + service_subnet 不与之重叠即可
#   (10.96.0.0/12 或 10.206.0.0/16 都行)。
#calico_dual_ippool_enabled: true
#calico_gpu_pool_cidr: "10.14.32.0/19"    # 必填(上一项为 true 时)
#calico_cpu_pool_cidr: "10.14.96.0/20"    # 必填(上一项为 true 时)
# blockSize = 每节点一次拿多少地址; 池内块数 != 可容纳节点数(节点 Pod 数超一块就再要一块)。
# 按 kubelet_max_pods=110 算: /28 每节点要 7 块, /26 要 2 块。GPU 节点别忘 DaemonSet 也占。
#calico_gpu_pool_blockSize: 28            # 每块 16 个地址
#calico_cpu_pool_blockSize: 26            # 每块 64 个地址
# cilium 路由: tunnel(overlay,默认) / native(直接路由,同二层)
cilium_routing_mode: tunnel
cilium_tunnel_protocol: vxlan           # tunnel 模式下: vxlan / geneve
cilium_kube_proxy_replacement: false    # true=eBPF 替换 kube-proxy(需内核>=5.10)
enable_multus: false                     # 是否叠加 multus 多网卡

# ---- 组件版本(与 k8s 版本无关, 按各自兼容矩阵挑) ----
# ⚠ etcd_version 不在这里写 —— 它与 k8s 版本强配套, 由 defaults.yaml 的 k8s_compat 按
#   k8s_version 自动派生(值 = kubeadm config images list 给的 etcd tag 剥掉 -N 后缀)。
#   下载脚本也用 kubeadm 校准同一个值, 两边才对得上; 在这里手写会盖掉派生值, 导致
#   下载落 etcd/v3.6.4/ 而部署去找 etcd/v3.6.8/ 报「Could not find」。
#   确需钉死某版本时才取消下行注释(并自行保证与 k8s 版本兼容):
#etcd_version: "3.6.4"
# ⚠ 换 calico 版本【不是只改这个数】: CRD 与 ClusterRole 规则在 7800+ 行的 vendored 上游
#   manifest 里, 本项只换镜像 tag。必须先为目标版本生成模板:
#     scripts/gen-calico-template.sh <版本>          # 先看 CRD/RBAC diff
#     scripts/gen-calico-template.sh <版本> --apply  # 确认后写入
#   模板按版本存放(roles/cni/templates/calico/<版本>.yaml.j2), 各站点可停在不同版本;
#   缺对应模板时 roles/cni 会明确报错并列出仓库现有版本。详见 docs/部署说明.md 十三.2。
calico_version: "3.32.2"
flannel_version: "0.26.7"
flannel_cni_version: "1.6.2-flannel1"    # flannel-cni-plugin 镜像 tag(拼进 flannel_images.flannel_cni)
cilium_version: "1.16.5"
helm_version: "3.18.5"                    # 不带 v; 脚本/模板会自动拼 helm-v{{helm_version}}
multus_cni_version: "4.2.4"
nodelocaldns_version: "1.26.4"           # NodeLocal DNSCache(k8s-dns-node-cache); registry.k8s.io/dns 上游 tag
# 是否启用 NodeLocal DNSCache(每节点本地 DNS 缓存, 查询链: Pod -> 169.254.25.10 -> CoreDNS)。
# true = 装机时随 CNI 之后自动部署 DaemonSet, 并把 kubelet 的 clusterDNS 指向它(与 kk 同款做法);
#        worker 由 kubeadm join 拉 kubelet-config ConfigMap 自动继承, 无需单独配。
# 不填/false = 不装, Pod 直连 coredns ClusterIP。
# 单绑 link-local 形态, iptables / ipvs 两种 kube-proxy 模式都适用。
enable_nodelocaldns: true                # 装机时随 CNI 之后自动部署, 并把 kubelet clusterDNS 指向它
# 下面两项默认值即可, 一般不用动:
#nodelocaldns_ip: "169.254.25.10"        # 改了要与各节点 kubelet clusterDNS 一致(由上面开关自动同步)
#nodelocaldns_coredns_svc: coredns       # 上游 CoreDNS Service 名。现已【自动探测】(依次试
                                         #   本项 -> kube-dns -> coredns), kubeadm 集群是 kube-dns、
                                         #   kk 集群是 coredns, 都不用手填; 只有用了别的名字才需指定
kube_criSocket: unix:///run/containerd/containerd.sock   # 默认 containerd; cri-dockerd 用 unix:///var/run/cri-dockerd.sock
dns_domain: cluster.local                # 集群内 DNS 域名(几乎不用改)
# kube_proxy_mode: ipvs(当前默认) / iptables / nftables
# ⚠ ipvs 上游已弃用(KEP-5495): 1.35 弃用 -> 1.40 默认禁用 -> 1.43 移除。新站点建议填 nftables。
# nftables 要求【内核 5.13+ 且 nft 1.0.1+】(1.33 GA)。填之前逐节点确认:
#   ansible all -m shell -a 'uname -r; nft --version'
#   麒麟/openEuler 等老内核可能不满足, 不满足就继续 ipvs 或 iptables。
#   ⚠ 非 drop-in: NodePort 地址绑定、从 localhost 访问 NodePort 的行为与 ipvs 有差异, 先在测试环境实测。
kube_proxy_mode: ipvs

# ---- apiserver 入口 / 高可用 VIP(这几项一体) ----
# 单 master: lbvip 保持注释, 入口自动取「首 master : apiserver_port」。
# 多 master: 必须填 lbvip, 入口变「lbvip : apiserver_lb_port」(>=2 台 master 没配会被 assert 拦)。
apiserver_port: 6443                     # apiserver 本地监听端口(LB 后端)
apiserver_lb_port: 8443                  # 经 VIP 访问的端口(kubeadm controlPlaneEndpoint + LB 前端; 仅跑 LB 时用到)
# lbvip: 172.171.0.250                   # 跑 LB([k8slb] 组)才取消注释填 VIP
vip_prefix: 22                           # VIP 掩码位数(仅配了 lbvip 时生效)
# VRRP 抢占策略: delay(默认, 故障切走 + 恢复后等 N 秒回切) / immediate / nopreempt(不回切)
# ⚠ nopreempt 走 FAULT 机制(不配 weight): 两台同时故障时期间【无人持 VIP】; delay 走降权,
#   总有一台持有。不回切的代价就在这里, 想要就自己开。详见 defaults.yaml 的那段注释。
#lb_vrrp_preempt_mode: nopreempt
#lb_vrrp_preempt_delay: 10               # 仅 delay 模式用; keepalived 允许 0-1000 秒
# kube_cp_endpoint_ip 与下面的 cp_endpoint_domain/target 【分工不同, 别混】:
#   kube_cp_endpoint_ip  —— 往【别人建的】域名 endpoint 集群加节点时用(典型: kk 建的
#     lb.kubesphere.local)。域名由 kube-add-node/master 从 join 命令里正则抠出, 本项只回答
#     "那个域名在新节点上解析到哪个 IP"。⚠ 值必须是 IP; 本仓库建的集群用不到它。
#   cp_endpoint_domain   —— 【本仓库建】域名 endpoint 集群时用(见下一节)。
# 二者互斥: 配了 cp_endpoint_domain 时上面那条路径会自动跳过(避免同一名字两个写入方)。
#kube_cp_endpoint_ip: 172.171.0.x        # 默认取 lbvip 或首 master IP

# ---- controlPlaneEndpoint 用域名(默认开) ----
# endpoint 在 kubeadm init 时写进 kubeadm-config ConfigMap 与 apiserver 证书 SAN, 【事后不可改】。
# 用域名: 以后换入口(补 VIP / 换网段)只需把解析指到新地址 —— 名字没变, 证书与 kubeconfig 不动。
# 用 IP  : 同样的事要改 ConfigMap + 重签证书 + 改每节点四份 kubeconfig(本仓库无对应工具)。
# 代价只是"每节点要有一行 hosts 解析", 由 kube-common 自动下发 —— 所以默认就用域名。
# ⚠ 每节点必须能解析它, 否则 kubelet 起不来。有内网 DNS 的站点也可以配在 DNS 里。
# ⚠ 别用 .local 结尾(mDNS 保留域)。本仓库约定 .local.clusters, 与 harbor 一致。
# 置空则回到旧行为(endpoint 直接用 IP)。
cp_endpoint_domain: lb.local.clusters
#cp_endpoint_target: 172.171.0.250       # 域名解析到的 IP。默认: 有 lbvip 用 lbvip, 否则首 master
                                         #   只有"要指向既不是 VIP 也不是首 master 的第三个地址"才写
                                         #   改了它之后跑 playbook/cp-endpoint-hosts.yaml 刷新全节点解析

# ============================================================================
#  两种目标形态(端口在 init 时定死, 选之前想清楚)
# ============================================================================
#
# 【形态 1】单 master, 以后靠 keepalived 加 VIP 做高可用 —— 端口 apiserver_port
#   起步(现在):
#     lbvip 不填; lb_proxy 不填
#     endpoint = lb.local.clusters:apiserver_port -> 解析到首 master
#     ⚠ 此时【没有】高可用: 首 master 挂了, 其余 master 的 apiserver 虽活着但没人找得到 ——
#       每节点四份 kubeconfig 都指着这个名字。域名买的是"以后能平滑换地址", 不是"现在就有 HA"。
#   升级(拿到一个同网段空闲 IP 之后):
#     lbvip: 172.171.0.250
#     lb_proxy: keepalived-only          # ⚠ 必填! 漏了它端口会跳到 apiserver_lb_port, 与已定死
#                                        #   的 endpoint 不一致, 变成"看着能用但 HA 是假的"
#     lb_vrrp_preempt_mode: nopreempt    # 可选: 故障切走后不回切
#     [k8slb] 填 2-3 台(可与 master 共机)
#     跑 lb-haproxy-install.yaml(该形态下只装 keepalived) + cp-endpoint-hosts.yaml
#     端口不变, 所以证书与 kubeconfig 一行不动 —— 这就是域名的收益所在。
#   ⚠ 这条路【永远加不进 haproxy】(它抢不到 apiserver_port), 也就是永远没有负载均衡。
#
# 【形态 2】3 master + keepalived + haproxy —— 端口 apiserver_lb_port, 负载均衡 + 高可用
#     lbvip: 172.171.0.250
#     lb_proxy 保持默认 haproxy
#     [k8slb] 填 2-3 台(可与 master 共机; haproxy 听 apiserver_lb_port, 与 apiserver 不冲突)
#     顺序: 先 lb-haproxy-install.yaml 再 k8s-install.yaml
#           (admin.conf 指向 endpoint, init 之后的 task 全靠 kubectl, VIP 不通会卡住)
#
# 两种形态【不能互换】: 端口不同, 而 endpoint 事后不可改。
#   形态 1 -> 形态 2 要重建集群; 形态 1 内部(无 VIP -> 有 VIP)不用, 那才是域名解决的事。
# 要负载均衡就直接上形态 2; 只要故障转移、机器暂时不够就走形态 1。
#
# ---- 非标准: lb_proxy: keepalived-only ----
# 给"已经建成 2 master 且拿不到 VIP"这类情况兜底 —— 只跑 keepalived、不装 haproxy,
# VIP 直接落到持有它那台的 apiserver。有故障转移, 【无负载均衡】。
# 唯一的实际价值: 端口与"无 VIP"状态一致(都是 apiserver_port), 所以配上域名后能从
# "域名指首 master"平滑升级到"域名指 VIP" —— 不重签证书、不改 kubeconfig。
# ⚠ 但这条路【永远加不进 haproxy】(端口在 init 时定死), 也就是永远没有负载均衡。
#   新建集群不要选它, 直接上标准形态。

# ---- kubelet 调优 ----
kubelet_cpu_manager_policy: "none"       # none(通用) / static(CPU 绑核, 仅 AI 物理机)
kubelet_cpu_align_by_socket: false       # 仅 static 且 sockets<=NUMA 节点数时才 true(否则 kubelet 起不来)
kubelet_max_pods: 110                    # AI 大节点可调大(注意 pod CIDR 每节点容量)
kubelet_kube_reserved_cpu: "200m"
kubelet_kube_reserved_mem: "512Mi"
kubelet_system_reserved_cpu: "200m"
kubelet_system_reserved_mem: "256Mi"
# 驱逐策略(内存阈值/宽限; 磁盘/pid 相关阈值在 kubeadm-config.yaml.j2 里固定)
kubelet_eviction_hard_mem: "5%"          # 硬驱逐: 低于此立即驱逐 Pod(无宽限)
kubelet_eviction_soft_mem: "10%"         # 软驱逐: 低于此且持续超宽限期才驱逐
kubelet_eviction_soft_grace: "1m30s"     # 软驱逐宽限期
kubelet_eviction_max_pod_grace: 120      # 驱逐时给 Pod 的最大优雅退出秒数
kubelet_eviction_pressure_transition: "30s"  # 退出驱逐压力状态的最短停留(防抖)
kubelet_pod_pids_limit: 50000            # 单 Pod 最大进程数(防 fork 炸弹); -1 不限。AI 训练/多进程负载易撞 1w 上限, 故给 5w
kubelet_container_log_max_size: "5Mi"    # 单容器日志文件上限
kubelet_container_log_max_files: 3       # 容器日志保留文件数
kubelet_shutdown_grace: "30s"            # 节点关机时给普通 Pod 的优雅退出时间
kubelet_shutdown_grace_critical: "10s"   # 其中留给关键 Pod 的部分
#kubelet_security_enhancement: false      # true=开 CIS 安全加固(关只读端口10255/protectKernelDefaults/TLS套件等)
# kubelet: podPidsLimit: -1 + serializeImagePulls: false
#   ⚠ -1 = 放弃单 Pod pid 上限(fork 炸弹可打满整机)。
#   ⚠ 二者互斥: 本项 true 时走 -1, 上面的 kubelet_pod_pids_limit 【不参与渲染】(成死值)。
#kubelet_unlimited_pids: true
# kubelet 的 client/serving 证书走 CSR, 由 controller-manager 的 --cluster-signing-duration
# 控制 —— 即本项。不设默认只有 8760h(1y), 与控制面证书(kubeadm 直签, 876000h)不一致。
kube_signing_duration: "876000h"         # =100y, 与控制面证书对齐; 改小需同步考虑轮换与审批
# 显式声明 kubelet serving 证书自动轮换(featureGates.RotateKubeletServerCertificate)。
# 该 gate 自 k8s 1.12 起默认即 true, 写出来不改变行为, 只是让轮换这件事在配置里可见,
# 并与现网 kk 集群写法一致。⚠ 已 GA-locked: 未来版本移除该 gate 后 kubelet 会启动失败,
# 届时把本项置 false 即可(行为不变, 只是不再显式声明)。
kubelet_rotate_server_cert_gate: true

# ---- 节点调优可选开关(默认全关, 需要的站点才打开) ----
# 这些与本仓库既有默认值部分冲突, 故做成开关, 不影响其它站点/现网集群。
# NVIDIA 驱动 modprobe 参数(EnableStreamMemOPs / PeerMappingOverride / 放开 profiling)
#   只下发文件 + 重建 initramfs, 【不自动重启】; reboot 后按 /proc/driver/nvidia/params 校验。
#gpu_nvidia_modprobe_tuning: true

# ---- etcd ----
etcd_install_dir: "/opt/etcd"            # 二进制目录
etcd_data_dir: "/var/lib/etcd"           # 数据目录
etcd_cert_expiry: "36500"                # 证书有效期(天)
etcd_cluster_token: "prod-etcd-cluster-01"
etcd_enable_metrics: true                # 暴露 etcd metrics 端点(listen-metrics-urls, 监控用; 有安全顾虑改 false)
etcd_config:
  quota_backend_bytes: 8589934592        # 8G
  auto_compaction_retention: "1h"
  auto_compaction_mode: periodic
  heartbeat_interval: 500
  election_timeout: 5000
  log_level: info

# ---- 在线下载源(离线 is_offline=true 时不用管) ----
# 默认值都在 defaults.yaml(均为「多源失败自动切换」的复数列表), 想换源才在这里【给完整列表】覆盖同名项。
# ⚠ 用复数名(k8s_bin_mirrors / gh_proxies / ...); 早期的单数名(k8s_bin_mirror / gh_proxy)已废弃, 代码不再读, 别再用。
#k8s_bin_mirrors:
#  - "https://files.m.daocloud.io/dl.k8s.io/release"
#  - "https://dl.k8s.io/release"
#etcd_bin_mirrors:
#  - "https://mirrors.huaweicloud.com/etcd"
#  - "https://files.m.daocloud.io/github.com/etcd-io/etcd/releases/download"
#gh_proxies:                              # github 代理(containerd/runc/crictl 的 *_bin_mirrors 由它派生)
#  - "https://ghfast.top/https://github.com"
#  - "https://ghproxy.net/https://github.com"


# ---- DNS(Debian 系初始化, 默认不接管) ----
# init_config_dns=false(默认): 不碰系统原有 DNS, 下面 dns/domains 不生效。
# 要 ansible 接管(写 systemd-resolved)时置 true, 并填好 dns/domains。
init_config_dns: false
dns:
- 114.114.114.114
- 223.5.5.5
domains: "local.clusters"


# ============================================================================
#  系统初始化调参(init / os-init / os-check)
#  os* 系是站点预设, 覆盖角色默认(见 roles/*/defaults); 当前为「青岛联通交付」取值, 换项目按需改。
# ============================================================================

# ---- 时间同步 & 时区 ----
# NTP 源, 内网无外网改内网 IP。init 与 os-init 共用此变量(osinit_ntp_servers 默认取它)。
chrony_ip: ntp1.aliyun.com
# 要配多源冗余时才显式覆盖(否则默认取上面 chrony_ip):
#   osinit_ntp_servers: [10.82.4.1, 10.82.4.2]

# ---- 自建内网 NTP 服务器(可选; 默认关闭)----
# 【开关就是 inventory 的 [chronyserver] 组】—— 组留空 = 本特性完全不生效, 所有节点
# 沿用原来的纯客户端行为(指向 chrony_ip), 现有站点不受任何影响。不需要就别管这一节。
#
# 要自建时: inventory 的 [chronyserver] 填一台机器名, 然后把下面 allow 网段取消注释填好。
#   · 那台机器: chrony 变成 server, 上游取 chrony_ip / osinit_ntp_servers, 并对下放行
#   · 其余节点: 自动改指向那台的 ansible_host(不用再动 chrony_ip)
#
# ⚠ 放行网段【必填】(填了 [chronyserver] 却不填这个, role 内 assert 会直接拦住):
#   chrony 默认拒绝所有请求, 漏了这项的症状是 server 上一切正常、客户端全是 ^? 不可达。
#chrony_allow_networks:
#  - 10.14.64.0/24
#  - 10.101.16.0/24
#
# server 失去上游时是否仍对下服务(默认 true)。置 true 才能保证【交付标准要的
# 「集群内最大时间差 <15 秒」】在外网不通时依然成立 —— 全集群一起跟着 server 的钟走。
# stratum 故意取较大值(越大越不权威), 真实上游恢复后客户端会自动优先真源。
#chrony_server_serve_when_unsynced: true
#chrony_server_local_stratum: 10
# 只在指定 IP 上监听 udp 123(默认空 = 监听所有地址)。多网卡机器建议钉到管理网 IP。
#chrony_server_bindaddress: 10.14.64.30

# ---- 云厂商元数据 NTP(init 角色模板末尾那行, 默认保留)----
# init 的 chrony.conf.j2 末尾一直有 `server 169.254.169.254 iburst prefer` —— 云厂商的
# 元数据地址。默认 true(保持原行为, 现有站点不受影响)。
# 裸金属机房里它永远不可达, 只是 chronyc sources 里多一条 ^? 噪音, 【不会】阻止其他源
# 被选中; 嫌碍事就置 false 去掉。
#chrony_cloud_metadata_ntp: false
# 时间没同步上不中断后续步骤(降级为 ⚠ 告警); os-check 侧仍判 FAIL, 交付报告会带出来。
# 找到可用内网 NTP 后改 chrony_ip 重跑: --tags time
osinit_fail_on_unsynced: false
# ⚠【偏离交付标准】标准要求 UTC, 本项目用 Asia/Shanghai(只影响显示, 不影响对时精度)。
#   下面两值必须一致(不一致 os-check 会误报 FAIL); 交付材料里应作为偏离项列出或与甲方确认。
osinit_timezone: Asia/Shanghai
oscheck_expect_timezone: Asia/Shanghai

# ---- 主机名 / hosts ----
# 不往 /etc/hosts 写全集群映射(本项目靠 IP 互访)。
# ⚠ 置 false 会把以前写进去的那个块【清掉】, 不是留着不管。
osinit_write_cluster_hosts: false

# ---- 统一登录账号 ----
osacct_user: wwxq                        # 账号名; 角色默认中性 osadmin, 青岛交付要求 wwxq
osacct_user_sudo_nopasswd: true          # sudo 免密(本项目运维习惯, 交付标准无要求)
# 删除除统一账号 / root 外的一般用户(uid>=1000)。
# ⚠【不可逆】state=absent + remove=yes, 家目录一起删。开之前确认 root 或统一账号能登进去。
#    想先看会删谁不真删: 保持 false 跑一遍看"多余一般用户盘点"那步输出。
osacct_remove_extra_users: true

# ---- 统一登录密码 ----
# 就写这里(本文件已 gitignore 不入库), 取消注释填实际密码, 直接跑 os-init.yaml, 不用 export:
#osacct_user_password: '实际密码'
# 替代: export OSACCT_USER_PASSWORD='实际密码'(别用 sudo 会清环境变量, 要 sudo -E)。
# ⚠ 只写一处: env.yaml 优先级 > 角色默认, 两处都写时环境变量永不生效。
# root 密码默认同统一密码; 要单独设: osacct_root_password: '另一个密码'
