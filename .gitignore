inventory/hosts
offline/artifacts/harbor/harbor.unisound.ai/certs/
offline/artifacts/harbor/harbor.local.clusters/
#playbook/roles/certs/files/etcd-default/
#./tmp/hosts
#./tmp/scale_hosts
#./tmp/prod.yaml


# 冗余的 harbor 镜像打包(部署用单个 harbor-*-v2.11.2.tar.gz,此文件无引用,不纳入git以省LFS配额)
offline/artifacts/harbor/x86/harbor.v2.11.2.tar.gz

# k8s: 生成的 etcd/CA 私钥证书,绝不入库
playbook/roles/certs/files/etcd-*/

# env.yaml 是每套环境的实际配置(由 init.sh 从 tmp/env.yaml.example 生成),不入库
inventory/group_vars/all/env.yaml

# 体检报告产物 (os-check)
# ⚠ .gitignore 拦不住 git add -f, 兜底靠 scripts/git-hooks/pre-commit(init.sh 自动装)
reports/

# 旧版 init.sh 生成过这个明文密码文件, 现在密码写在 env.yaml 里, 已不再生成。
# 2026-08 两个仓库本地残留的那份已删掉。这行【别删】—— 纯兜底: 万一从旧备份或
# 别人的旧工作副本里又冒出来一份, 至少不会被 git add . 带进库。
# (旧名 inventory/qdlt-secrets.yml 也一并保留兜底)
inventory/os-secrets.yml
inventory/qdlt-secrets.yml
