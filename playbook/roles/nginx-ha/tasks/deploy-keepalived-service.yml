- name: 部署 keepalived.service（仅当变更）
  template:
    src: keepalived.service.j2
    dest: /etc/systemd/system/keepalived.service
    owner: root
    group: root
    mode: "0644"
  notify: daemon-reload
