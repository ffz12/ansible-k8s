节点,节点类型,带内IP,判定,分类,检查项,期望,实际
{% for h in osreport_hosts %}
{%- for i in hostvars[h].oscheck_results %}
"{{ h }}","{{ hostvars[h].node_role | default('cpu') | upper }}","{{ hostvars[h].mgmt_ip | default('') }}","{{ i.verdict }}","{{ i.cat }}","{{ i.item }}","{{ i.expect | replace('"', '""') }}","{{ i.actual | replace('"', '""') }}"
{% endfor %}
{%- endfor %}
