# 版本说明

本仓库的版本档案。每个版本记三样：**代码在哪个 commit**、**站点配置快照**、**已知状态与限制**。

## 为什么需要这份文档

代码可以靠 git tag 固化，但 **`inventory/hosts` 与 `inventory/group_vars/all/env.yaml` 是 gitignored 的**（`.gitignore:1` 与 `:17`）。同一个 tag 配不同的 `env.yaml` 能建出完全不同的集群（k8s 版本、CNI、网段、有无 lbvip 全在里面）。所以「生产跑的是 v1.0.0」这句话只说了一半 —— 站点配置必须在这里另记一份。

版本号用语义化版本 `vMAJOR.MINOR.PATCH`：

| 位 | 什么时候进 | 例子 |
|---|---|---|
| PATCH | 修 bug，行为不变 | 修一个渲染错误、补一句注释 |
| MINOR | 加能力，旧配置照旧能跑 | `kube_proxy_mode` 支持 nftables（默认不变） |
| MAJOR | 默认行为变了 / 有单向门 | calico 封装默认 `none` → `ipip`；`controlPlaneEndpoint` 改为无条件写 |

**打版本的时机是「交付/固化」，不是每个 commit。** 日常粒度 git 历史已经够了，版本号只标那些"某个站点从此跑这套"的节点。

---

## v1.0.0 — 生产基线（cluster A / cn-10-14-73-1）

固化日期 **2026-09-16**。这是第一个版本号，仓库此前从未打过 tag。

### 代码

```
commit  09cf9720ac7d57ab6bc1079b1dfd0e4061abe3f2
short   09cf972
日期    2026-09-09 21:03:43 +0800
作者    ffz521
标题    init/sysctl: 13 项交付标准参数合并进 sysctl.conf, 删掉一直被覆盖的 drop-in
```

生产控制节点 `cn-10-14-73-1` 的 `/root/ansible-k8s` 停在此处，**且不再更新代码**。

这个 commit 上有两个引用，分工不同：

| 引用 | 类型 | 作用 | 会不会动 |
|---|---|---|---|
| `v1.0.0` | annotated tag | 不可变档案点，`git checkout v1.0.0` 永远回到固化那一刻 | **不动** |
| `release/v1.0` | 分支 | 生产克隆跟踪的目标 | 冻结快照，暂不动 |

**分支是 2026-09-17 补的，为了解决一个真实隐患**：那份克隆原先跟踪 `origin/master`，而远端默认分支在 2026-09-09 与 09-15 之间改名成了 `main`，`master` 已被删除（`git ls-remote --heads origin` 现在只有 `main` 与 `release/v1.0`）。所以"生产不更新代码"此前是靠"跟踪了一个不存在的分支"这个巧合维持的，不是靠设计 —— 哪天有人把它切到 `main`，一次 `git pull` 就会跳 47 个 commit，而那些改动多数没在真机验过。

生产克隆切到该分支（在 `cn-10-14-73-1` 上执行）：

```bash
cd /root/ansible-k8s
git fetch origin
git checkout -B release/v1.0 origin/release/v1.0
git branch -vv        # 确认跟踪 origin/release/v1.0
```

切过去之后 `git pull` 拉的是这条分支 —— 不动它就永远是 `09cf972`。

⚠ 该分支是**冻结快照**，不往上打补丁。以后生产真要修某个问题，再从它开一条维护分支、只 cherry-pick 需要的 commit —— 那时需求具体，比现在预留一条能动的分支更稳（分支一旦能动就会有人想动）。

### 站点配置快照

采集于 2026-09-17（`env.yaml` 不入库，故在此留档）。**只记决定集群形态的项，口令类一律不录。**

| 项 | 值 |
|---|---|
| `k8s_version` | `1.36.3` |
| `containerd_version` | `2.2.1`（config v3） |
| `runc_version` | `1.1.12` |
| `is_offline` | `true` |
| `kube_criSocket` | `unix:///run/containerd/containerd.sock` |
| `kube_proxy_mode` | `ipvs` |
| `kube_network_plugin` | `calico` |
| `calico_version` | `3.32.2` ⚠ 见下方「已知状态与限制」第 4 条 |
| `calico_encapsulation` | `ipip` |
| `calico_ip_autodetection_method` | `interface=bond0` |
| `calico_dual_ippool_enabled` | `true` |
| `pod_subnet` | `10.14.128.0/18` |
| `calico_gpu_pool_cidr` | `10.14.128.0/19` |
| `calico_cpu_pool_cidr` | `10.14.160.0/19` |
| `service_subnet` | `10.206.0.0/16` |
| `dns_domain` | `cluster.local` |
| `enable_nodelocaldns` | `true`（`nodelocaldns_version` `1.26.4`） |
| `enable_multus` | `false` |
| **入口** | |
| `lbvip` | `10.14.73.200` |
| `vip_prefix` | `24` |
| `apiserver_port` / `apiserver_lb_port` | `6443` / `8443` |
| endpoint（推导） | `10.14.73.200:8443` —— 即 haproxy 形态（本文档写作时 `lb_proxy` / `cp_endpoint_domain` 两个变量还不存在，等价于 `lb_proxy: haproxy` + 无域名） |
| **kubelet** | |
| `kubelet_max_pods` | `500` |
| `kubelet_cpu_manager_policy` | `none` |
| `kubelet_unlimited_pids` | `true`（`podPidsLimit: -1`；此时 `kubelet_pod_pids_limit` 不参与渲染） |
| 驱逐阈值 | 硬 `10Gi` / 软 `20Gi`（宽限 `2m`） |
| **etcd** | |
| `etcd_install_dir` / `etcd_data_dir` | 均为 `/var/lib/container/etcd` |
| `etcd_cluster_token` | `prod-etcd-cluster-01` |
| `etcd_config` | quota 8G / compaction `1h` periodic / heartbeat 500 / election 5000 |
| **其它** | |
| `harbor_instances` | `harbor.local.clusters` → `10.14.73.3` |
| `chrony_ip` | `10.14.73.1`（allow `10.14.64.0/24`、`10.14.73.0/24`） |
| `osacct_user` | `wwxq`（`osacct_remove_extra_users: true`） |
| `connect_as` | `root` |

不录的项：`harbor_admin_password`、`password`（harbor 数据库）、`apt_proxy_url`。

⚠ **更正一处归属**：`c298904`（haproxy 加 `retries` + `redispatch`）的 commit message 里写「对照使用者贴出的现网生产配置(cn-10-14-73-1)」—— **那是错的**。那份 `bind *:16443` / 后端 `10.226.16.1-3` 的 haproxy.cfg 是**参考样例**，不属于本站点：本站点 `lbvip` 是 `10.14.73.200`、`apiserver_lb_port` 是 `8443`，且 `10.226.16.x` 不在 `chrony_allow_networks`（`10.14.64.0/24`、`10.14.73.0/24`）任何一段里。该 commit 的技术结论不受影响（`retries` / `redispatch` 缺失是模板层面的，各站点渲染自同一份 `haproxy.cfg.j2`），只是"对照的是谁"记错了。已推送的 commit message 无法修改，故在此更正。

`inventory/hosts` 的分组（各组几台、GPU 节点的 `accelerator_type`）同样不入库，尚未采集。

### 已知状态与限制

以下三条是 `09cf972` **之后**才修的，生产的代码库里没有 —— 从这个 checkout 重跑相关 playbook 会退回旧行为：

**1. keepalived 仍是多播配置（最要紧）**

`55937c0 haproxy-ha/keepalived: VRRP 默认改单播 + 抢占策略可选 + 修 weight/auth_pass/state 三处失效配置` 不在本版本内。

即从此 checkout 重跑 `lb-haproxy-install.yaml`，渲出来是**多播 + `state MASTER` 写死 + `nopreempt` 实际失效 + `auth_pass` 被 8 字符截断**的那套 —— 也就是现网出现过永久双 MASTER（脑裂）的配置。

⚠ **生产机器上的 keepalived 配置可能已手工改成单播，与本 tag 的代码不一致。** 重跑 LB 相关 playbook 会把手工改动覆盖回多播。动 LB 之前先确认机器现状。

**但 `55937c0` 不是纯粹的修复 —— 它同时把 `weight` 改坏了。** 该 commit 把 `weight -30` 改成
`default(-5)`，而 keepalived 是把 weight 加到**本节点自己**的 priority 上、VRRP 再选**最高**者：
`-5` 时首台脚本失败只从 100 掉到 95，仍是最高 → **VIP 不动，健康检查等于装饰**。

所以这一条对本版本反而是**有利**的：`v1.0.0` 的 `weight` 还是 `-30`，在 2-3 台 LB 下选主正确，
**生产不受该回归影响**。`main` 侧已由 `1d9fea9` 改为按台数派生 `-(N × 10)` 修正。

**2. `k8s_compat` 缺 1.35 行 —— 本站点不受影响**（已核实）

`07c64c2 k8s_compat: 补 1.35 行` 不在本版本内，本版本的表只有 **1.28 / 1.34 / 1.36** 三行。

而本站点 `k8s_version: 1.36.3` → 表里有 `1.36` 行，`crictl_version` 取 `1.36.0`（crictl 上游确实只发 `x.y.0`），不会撞 404。**本条不适用。**

**3. calico 封装默认值不同 —— 本站点不受影响**（已核实）

`facdfb8 calico: 封装默认从 none 改为 ipip` 不在本版本内，本版本默认 `none`。

而本站点 `env.yaml` 显式写了 `calico_encapsulation: ipip`，以站点值为准。**本条不适用。**

**4. calico 的 manifest 结构与镜像版本错开 —— 已在生产核实**

填快照时发现的，不在原先三条里。**已核实（2026-09-17，取自生产 `cn-10-14-73-1` 的 `/etc/kubernetes/calico.yaml`）：**

```
文件第 2 行:  # calico.yaml.j2 —— 由 Calico 官方 v3.31.3 manifest 对齐生成
5 处镜像:     harbor.local.clusters/kubernetes/calico-{cni,node,kube-controllers}:v3.32.2
```

即 **manifest 结构是官方 v3.31.3 的形状，镜像 tag 是 v3.32.2**。已稳定运行数日。

成因：v1.0.0 只有一份 `calico.yaml.j2`（结构对齐 3.31.3，`defaults.yaml` 的 `calico_version` 兜底也是 `3.31.3`），而站点 `env.yaml` 写 `3.32.2` —— 该变量只影响镜像 tag（`defaults.yaml:206-208` 拼 `:v{{ calico_version }}`），不影响结构。

`main` 侧已把模板按版本存放（`f413eee` / `8384ffd`，`calico/3.31.3.yaml.j2` 与 `calico/3.32.2.yaml.j2` 共存），并加了「模板生成版本与 `calico_version` 不一致即报错」的 guard（`ceafa50`）。**那个 guard 不在 v1.0.0 内**（`git log v1.0.0 | grep ceafa50` 为空），所以在生产这个版本上这种错开是**静默**的 —— 跑了这么久没人知道两者不一致。

⚠ **风险在"以后动 CNI"那一刻**：生产哪天升级代码到含 `ceafa50` 的版本、再跑 `k8s-cni.yaml`，会被那个 guard 直接拦下。届时两条路：

- `calico_version` 改回 `3.31.3` —— 与 manifest 对齐，但镜像回退
- 用 `main` 侧的 `calico/3.32.2.yaml.j2` —— 结构与镜像都是 3.32.2，但这是一次真实的 CNI 变更，需要窗口

**结论：生产保持现状不动最稳。要动 CNI 就得连带处理版本对齐，不能当成一次普通重跑。**

判断任一集群的 manifest 出自哪份模板 —— 看第 2 行即可（模板头部用的是普通 `#` 注释而非 `{# #}`，渲染时原样保留）：

```bash
sed -n '2p' /etc/kubernetes/calico.yaml
grep -c "image:" /etc/kubernetes/calico.yaml
```

⚠ 该文件是**部署时**的渲染产物。若有人后来 `kubectl edit/patch` 改过 DaemonSet，文件不会跟着变 —— 那种情况要看 `kubectl -n kube-system get ds calico-node -o yaml`。

**5. 两个已废弃变量仍留在 `env.yaml` 里**

`node_tuning_sysctl: true` / `node_tuning_keep_higher_net_buffers: false` —— 这两项在本版本里**零代码引用**（已核 `git grep -l node_tuning_sysctl v1.0.0 -- 'playbook/*'` 为空），`defaults.yaml:304` 那段注释也标了「已废弃(2026-09-09)」。那 13 项 sysctl 已合并进 `roles/init/files/sysctl.conf`。

留着无害但会误导 —— 读 `env.yaml` 的人会以为置 `true` 起了作用。改 sysctl 请直接改 `files/sysctl.conf`。

### 与后续版本的差距

本版本落后 `main`（`6f4cee5`，2026-09-16）**25 个 commit**。除上面三条外，其余较大的：

- `d81b8fc` / `deab5ab` / `ceafa50` / `f413eee` / `8384ffd` / `93b7043` / `10df0fc` — calico 双池的 hardware 标签修复、池名变量化、模板按版本存放（3.31.3 与 3.32.2 共存）
- `82bdde4` / `068695a` — 兜底版本抬到 k8s 1.35.8 / containerd 2.3.5 / calico 3.32.2 / runc 1.5.1
- `9ba9110` / `d40f0a1` / `5f0b35c` — kubelet.service 的 `TasksMax`、hardware 标签三处缺口
- `43d3a4c` — 两条只读 shell 加 `check_mode: false`，修 `--check` 必然失败
- `d52252f` — 离线 apt 源目录权限（`apt-get update` 报 unknown reason）
- `10c1ccd` — **`controlPlaneEndpoint` 改为无条件写**（单 master 集群从此不再堵死"以后加 master"；本版本建的单 master 集群仍有那个不可逆限制）
- `baef7ae` — `kube_proxy_mode` 支持 nftables（默认仍 ipvs）
- `98c2b79` — nginx-ha 改为按发行版包安装，弃用自建二进制与 `/data` 安装目录
- `734a940` — 多 master 无 lbvip 的预检降级为可放行
- `5c6aa58` / `6f4cee5` — 文档：`[master_to_delete]` 的作用域、两种 HA 方案

完整列表：`git log --oneline v1.0.0..main`

### 如何回到这个状态

```bash
git checkout v1.0.0           # 只是代码; env.yaml / inventory/hosts 仍需按上面快照另配
git show v1.0.0               # 看 tag 说明
```
